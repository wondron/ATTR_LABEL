# Conda 配置；环境名留空时使用 PATH 中的 python3。
CONDA_BASE_DIR=/root/miniconda3
CONDA_ENV_NAME=wondron

# 初始数据目录可写相对项目根目录的路径，也可写服务器绝对路径；含空格时加双引号。
LABEL_DATA_DIR=10-temp_label

# 网页允许浏览的数据根目录。多个绝对路径用英文冒号分隔；留空时仅允许初始目录。
LABEL_ALLOWED_DATA_ROOTS=

LABEL_HOST=0.0.0.0
LABEL_PORT=8577
